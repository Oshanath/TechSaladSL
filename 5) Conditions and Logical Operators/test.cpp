#include <iostream>

int main(){

    int a = 5; // assignment operator
    int b = 10;

    a = a + 10; // arithmetic operator (+ - * / %)
    std::cout << a << "\n";

    std::cout << b - a << "\n";

    std::cout << a * b << "\n";

    std::cout << a / b << "\n";

    std::cout << 93 % 10 << "\n"; // modulus, remainder

    int g = 5;
    int h = -g;  // negation is a unary operator

    // arithmetic operators, logical operators, bitwise operators
    // unary, binary, ternary

}